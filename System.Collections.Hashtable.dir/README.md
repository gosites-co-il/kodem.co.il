# System.Collections.Hashtable.name

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build System.Collections.Hashtable.name` to build the library.

## Running unit tests

Run `nx test System.Collections.Hashtable.name` to execute the unit tests via [Jest](https://jestjs.io).
