import { ChannelsCatalogView } from '../../../../../components/integrations/channels-catalog-view';

/** Settings route reuses the same Channels UI. */
export default function SettingsChannelsPage() {
  return <ChannelsCatalogView />;
}
